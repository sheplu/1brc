[package]
name = "obrc"
version = "0.1.0"
edition = "2021"

# The challenge forbids external dependencies. This section stays empty.
[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = 1
